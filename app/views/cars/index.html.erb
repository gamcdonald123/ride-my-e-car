<div class="header text-center">
<h1 class="p-3 bg-info bg-opacity-10 border border-info border-start-0 rounded-end mb-3">🚗 🚙 🚘 List of Cars available to rent in your area 🚗 🚙 🚘 </h1>
</div>
<div class="container text-center">
  <div class="row" style="gap: 1rem;">
    <% @cars.each do |car| %>
      <div class="col-6 col-lg-3 card shadow p-3" style="width: 18rem;">
        <img src="<%= car.image_url %>" class="card-img-top" alt="...">
        <div class="card-body">
          <h3 class="card-title"> <%="#{car.make} #{car.model}" %> </h3>
          <ul class="list-unstyled">
            <li> Year: <%= car.year %> </li>
            <li>Rate: £<%=car.price %> </li>
            <%= link_to"view & book",car_path(car), class: 'btn btn-primary' %>
          </ul>
        </div>
      </div>
    <% end %>
  </div>
</div>
